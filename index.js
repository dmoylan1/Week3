var colors = [];
colors.push('Red');
colors.push('Orange');
colors.push('Yellow');
colors.push('Green');
colors.push('Blue');
colors.push('Purple');

for (let i = 0; i < colors.length; i++) {
    console.log(colors[i]);
}

for (names of colors) {
    console.log(names);
}
